export enum ErrorMessage {
  UNAUTHORIZED = 'Make sure that the user is providing the correct credentials for authentication',
  CONFLICT = 'A user with this email already exists',
  NOT_FOUND = 'User with this id not found',
  // FORBIDDEN_ERROR = 'Access Denied',
}
